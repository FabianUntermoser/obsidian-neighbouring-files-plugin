---
"neighbouring-files": minor
---

Add a mobile-only floating action button (FAB) with configurable gestures: swipe left/right/up/down to run commands, tap and double-tap actions, long-press to open the FAB settings, and long-press + drag to reposition the button. Gesture commands, haptics, and button position are configurable in the plugin settings under Mobile.
